# AGENTS.md

## 负责人信息

- 姓名：武文杰
- 学校：南京林业大学
- 身份：研究生
- 专业：电子信息

## 项目概览

这是一个单文件的静态前端项目，当前核心页面为 `index.html`。页面主题是“点点肌美容院”，定位为美容院宣传与预约落地页，面向希望了解服务、查看环境、提交预约或咨询的用户。

## 技术栈与实现方式

- 使用原生 HTML、CSS、JavaScript 编写
- 没有发现前端框架、构建工具或后端服务代码
- 页面样式直接写在 `index.html` 内部的 `<style>` 中
- 页面交互直接写在 `index.html` 内部的 `<script>` 中
- 图片主要使用 Unsplash 外链资源

## 当前页面内容结构

`index.html` 目前是一个完整的单页官网，主要包含以下区域：

1. 顶部导航
   提供品牌名“点点肌美容院”、页面锚点跳转和“立即预约”按钮。
2. 首屏 Hero 区域
   展示品牌定位、主标题、简介文案、预约按钮、服务入口、评分与护理数据，以及一个快速预约表单。
3. 服务价目区
   展示基础面护、小气泡清洁、肩颈 SPA、美甲美睫等项目及起始价格。
4. 安心标准区
   介绍护理流程、肤况沟通、器具消毒、分层护理和护理后建议等内容。
5. 空间实拍区
   用图片网格展示护理场景、环境和服务细节。
6. 用户评价区
   展示顾客反馈，强化信任感。
7. 联系咨询区
   显示电话、营业时间、地址，并提供咨询表单。
8. 页脚
   展示版权信息和品牌关键词。

## 交互逻辑

- 页面包含两个表单：
  - 首屏快速预约表单
  - 联系咨询表单
- 两个表单当前都没有接入后端提交接口
- 提交表单时会阻止默认提交行为
- 提交后会弹出一个 toast 提示：“已收到信息，顾问会尽快联系你确认。”
- 提示出现后，表单会被自动清空

## 视觉与样式特点

- 整体风格偏柔和、轻疗愈、生活方式化
- 主色包含薄荷绿、珊瑚色、浅金色、浅天蓝和米白色
- 页面使用较多圆角、阴影、渐变和卡片化布局
- 已包含响应式设计，针对 `980px` 和 `680px` 以下宽度做了移动端适配

## 项目资源与依赖判断

- 当前仓库可见文件很少，核心代码集中在 `index.html`
- `.gitignore` 中包含 `node_modules/`、`dist/`、`build/` 等常见忽略项
- 但从当前目录内容判断，项目现在并未实际使用 npm 依赖或构建产物
- 更适合直接作为静态 HTML 页面预览和继续开发

## 运行与预览方式

当前项目最简单的使用方式：

- 直接用浏览器打开 `index.html`
- 或使用任意静态服务器在本地启动后访问页面

由于没有发现构建脚本、配置文件或包管理文件，现阶段不需要编译步骤。

## 当前已知内容与注意事项

- 页面文案、地址和品牌内容都已经替换为美容院主题
- 地址文本当前写的是“南京林业大学”，这更像是占位信息，如果项目将来用于真实商用，建议替换为实际门店地址
- 所有表单当前仅做前端演示，没有数据持久化能力
- 图片依赖外链，离线环境下可能无法正常显示
- 项目所有代码集中在单个 HTML 文件中，后续如果继续扩展，建议按 HTML、CSS、JS 拆分，便于维护

## 后续维护建议

- 如果要继续完善，可以优先补充真实预约接口或表单校验
- 如果页面还会继续扩展，建议拆分为独立的样式文件和脚本文件
- 如果要正式部署，建议把外链图片替换为自有静态资源，减少加载不稳定风险
- 如果要做品牌化升级，可以补充 logo、门店信息、地图和真实案例内容
